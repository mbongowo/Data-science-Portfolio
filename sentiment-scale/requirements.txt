# pip fallback. Prefer `pixi install` (conda-forge) for the Spark/Arrow stack —
# pyspark pulls a JVM and the Arrow wheels can be fragile on some platforms.
#
#   python -m venv .venv && . .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#   pip install -e .

numpy>=1.24
pandas>=2.0
pyyaml>=6.0
pyspark>=3.4
pyarrow>=14.0
scikit-learn>=1.3
matplotlib>=3.7

# Lexicon model (loaded lazily by the scorer)
vaderSentiment>=3.3

# Dev / CI
pytest>=8.0
ruff>=0.6
mypy>=1.10
pre-commit>=3.7
