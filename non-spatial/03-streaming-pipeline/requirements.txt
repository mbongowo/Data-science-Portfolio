# pip fallback. The tested core (AQI + windows + alert engine + demo) needs only
# numpy/pandas/pyyaml; the rest powers the Kafka + Spark streaming layer, the
# Open-Meteo ingest, the DuckDB sink, and the Streamlit dashboard. Prefer a
# conda env (environment.yml) for the streaming stack — pyspark pulls a JVM.
#
#   python -m venv .venv && . .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#   pip install -e .

numpy>=1.24
pandas>=2.0
pyyaml>=6.0

# Streaming layer (Open-Meteo ingest + Kafka producer/consumer + Spark + sink)
requests>=2.31
kafka-python>=2.0
pyspark>=3.5
duckdb>=1.0
pyarrow>=15.0
streamlit>=1.36

# Azure path (opt-in, costs money):
# azure-eventhub>=5.11

# Dev / CI
pytest>=8.0
ruff>=0.6
mypy>=1.10
