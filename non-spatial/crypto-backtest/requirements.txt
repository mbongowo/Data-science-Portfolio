# pip fallback. Prefer `pixi install` (conda-forge) for the full stack —
# Polars and PySpark resolve more predictably from conda-forge, and PySpark
# also needs a JVM that conda-forge can provide.
#
#   python -m venv .venv && . .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#   pip install -e .

numpy>=1.24
pandas>=2.0
pyyaml>=6.0
pyarrow>=14.0
matplotlib>=3.7

# Large-scale resampling (optional; only scale_pipeline.py imports these)
polars>=0.20
pyspark>=3.5

# Dev / CI
pytest>=8.0
ruff>=0.6
mypy>=1.10
pre-commit>=3.7
