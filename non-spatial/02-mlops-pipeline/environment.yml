name: mlops-pipeline
channels:
  - conda-forge
dependencies:
  - python>=3.10,<3.13
  # Core (always needed)
  - numpy>=1.24
  - pandas>=2.0
  - pyyaml>=6.0
  # Heavy group
  - scikit-learn>=1.3
  - mlflow>=2.10
  - fastapi>=0.110
  - uvicorn>=0.27
  - prefect>=2.16
  - matplotlib>=3.8
  # Dev / CI
  - pytest>=8.0
  - ruff>=0.6
  - pip
  - pip:
      # evidently resolves more cleanly from PyPI.
      - evidently>=0.4
      # Azure path (opt-in, costs money):
      # - azure-ai-ml>=1.12
      # - azureml-mlflow>=1.55
