# pip install. The pure-numpy core (mlpipe.drift, mlpipe.metrics, mlpipe.model,
# mlpipe.features, mlpipe.demo) and the whole test suite need only the first
# three lines; everything below powers the opt-in MLOps plumbing (tracking,
# serving, drift dashboards, orchestration) and is imported lazily.
#
#   python -m venv .venv && . .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#   pip install -e .

# Core (always needed)
numpy>=1.24
pandas>=2.0
pyyaml>=6.0

# Heavy group: training, tracking, serving, drift dashboards, orchestration.
scikit-learn>=1.3
mlflow>=2.10
fastapi>=0.110
uvicorn>=0.27
evidently>=0.4
prefect>=2.16
matplotlib>=3.8

# Azure ML path (opt-in, costs money):
# azure-ai-ml>=1.12
# azureml-mlflow>=1.55

# Dev / CI
pytest>=8.0
ruff>=0.6
mypy>=1.10
pre-commit>=3.7
