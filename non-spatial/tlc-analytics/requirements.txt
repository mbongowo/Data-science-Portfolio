# pip fallback. Prefer `pixi install` (conda-forge) for a clean Spark/DuckDB
# environment. The pure-pandas reference mart needs only numpy/pandas/pyyaml.
#
#   python -m venv .venv && . .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#   pip install -e .

numpy>=1.24
pandas>=2.0
pyyaml>=6.0
pyarrow>=14.0

# Engines (heavy; only needed to actually run the bake-off)
duckdb>=0.10
pyspark>=3.5

# CLI
typer>=0.12

# Dev / CI
pytest>=8.0
ruff>=0.6
mypy>=1.10
pre-commit>=3.7
