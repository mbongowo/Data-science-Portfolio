# pip fallback. The tested core (model + metrics + data + recommend + demo) and
# the deployed app need only numpy/pandas; pyyaml reads config/config.yaml. The
# heavy group powers the optional sklearn RandomForest on the real Kaggle
# dataset, the Streamlit app and notebooks.
#
#   python -m venv .venv && . .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#   pip install -e .

numpy>=1.24
pandas>=2.0
pyyaml>=6.0

# App + optional stronger model + notebooks
scikit-learn>=1.3
joblib>=1.3
streamlit>=1.32
matplotlib>=3.7
jupyterlab>=4.0

# Dev / CI
pytest>=8.0
ruff>=0.6
