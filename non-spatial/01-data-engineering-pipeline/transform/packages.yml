# dbt_utils provides generate_surrogate_key and the accepted_range test used in
# the marts. Run `dbt deps` (from the transform/ directory) before `dbt build`.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
