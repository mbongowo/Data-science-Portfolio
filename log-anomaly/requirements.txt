# pip fallback. Prefer `pixi install` (conda-forge) for the Spark stack —
# pyspark pulls a JVM that conda-forge ships cleanly across platforms.
#
#   python -m venv .venv && . .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#   pip install -e .

numpy>=1.24
pandas>=2.0
pyyaml>=6.0
typer>=0.12

# Scale-out parsing + ML detector (optional; heavy)
pyspark>=3.5
pyarrow>=14.0
scikit-learn>=1.3

# Plotting
matplotlib>=3.7

# Dev / CI
pytest>=8.0
ruff>=0.6
mypy>=1.10
pre-commit>=3.7
