# Local install:
#
#   python -m venv .venv && source .venv/bin/activate   # (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#   streamlit run app/main.py
#
# Runtime dependencies live in app/requirements.txt, which is also the file
# Streamlit Community Cloud installs (it sits next to the entrypoint app/main.py).
# That file installs eo-monitor from this repo, so no separate step is needed.
-r app/requirements.txt

# Dev and CI tools, not needed at runtime.
pytest>=8.0
ruff>=0.4
pre-commit>=3.7
