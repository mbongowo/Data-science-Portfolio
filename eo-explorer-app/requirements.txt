# pip fallback for environments without pixi/conda.
#
#   python -m venv .venv && source .venv/bin/activate   # (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#   pip install -e ../eo-monitor          # reused flagship index package
#   streamlit run app/main.py
#
streamlit>=1.32
leafmap>=0.30
folium>=0.16
pystac-client>=0.7
odc-stac>=0.3.8
rioxarray>=0.15
xarray>=2024.1
rasterio>=1.3
numpy>=1.24
matplotlib>=3.7
dask>=2024.1
localtileserver>=0.10

# Dev / CI extras
pytest>=8.0
ruff>=0.4
pre-commit>=3.7

# Reused flagship index package (choose one form):
#   git form:
# eo-monitor @ git+https://github.com/JosephMbuh/eo-monitor.git
#   local editable (sibling repo):
# -e ../eo-monitor
