# Pip fallback. Prefer `pixi install` (conda-forge) for the geospatial stack,
# which resolves GDAL/GEOS/PROJ system libraries cleanly. This file mirrors
# pyproject.toml for environments where only pip is available.
osmnx>=1.9
networkx>=3.2
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
h3>=3.7
rasterstats>=0.19
rasterio>=1.3
matplotlib>=3.8
contextily>=1.5
folium>=0.15
numpy>=1.26
pandas>=2.1
pyyaml>=6.0
requests>=2.31
tqdm>=4.66

# dev
pytest>=8.0
ruff>=0.5
mypy>=1.10
pre-commit>=3.7
