# Outputs (regenerated by the demo / pipeline)
outputs/*
!outputs/.gitkeep

# Notebook-run demo artifacts
notebooks/outputs/

# Model checkpoints / weights (large, reproducible by re-training)
models/
*.pth
*.ckpt

# Downloaded imagery / datasets (EuroSAT etc.)
# NOTE: no blanket `data/` rule — data/.gitignore is tracked so the folder shows.
*.tif
*.tiff
*.zip

# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# Pixi / conda local env dirs
.pixi/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/
