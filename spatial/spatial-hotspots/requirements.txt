# pip fallback. Prefer `pixi install` (conda-forge) for the geospatial stack —
# GDAL/GEOS/PROJ wheels via pip can be fragile on some platforms.
#
#   python -m venv .venv && . .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
#   pip install -e .

numpy>=1.24
pandas>=2.0
pyyaml>=6.0
requests>=2.31
geopandas>=0.14
shapely>=2.0
libpysal>=4.9
esda>=2.5
splot>=1.1.5
mapclassify>=2.6
matplotlib>=3.7

# Optional GWR
mgwr>=2.2

# Dev / CI
pytest>=8.0
ruff>=0.6
mypy>=1.10
pre-commit>=3.7
