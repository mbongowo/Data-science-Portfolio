# pip fallback (mirrors pixi.toml / pyproject dependencies).
# Prefer `pixi install` for the conda-forge geospatial builds; this file exists
# so the project is installable in a plain virtualenv too:
#     python -m venv .venv && pip install -r requirements.txt
#
# Note: rioxarray/odc-stac pull in GDAL, which is far easier via conda-forge.

numpy>=1.24
scipy>=1.10
pandas>=2.0
statsmodels>=0.14
xarray>=2023.1
dask>=2023.1
rioxarray>=0.13
rasterio>=1.3
geopandas>=0.13
odc-stac>=0.3
pystac-client>=0.7
planetary-computer>=1.0
ruptures>=1.1
matplotlib>=3.7
pyyaml>=6.0

# dev
pytest>=7.0
ruff>=0.4
mypy>=1.8
pre-commit>=3.5
