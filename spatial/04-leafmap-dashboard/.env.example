# Example environment file for the clinic-access dashboard.
#
# The demo and the bundled sample app need no secrets. If you later wire in a
# live facility source (e.g. Healthsites.io), put its key here and copy this
# file to `.env`.

# HEALTHSITES_API_KEY=your-key-here
