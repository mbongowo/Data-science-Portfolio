# Notebooks

Optional exploratory notebooks (kept out of the test/CI path).

Suggested:
- `01_explore_data.ipynb` — visualise tiles, masks, per-band stats.
- `02_inspect_predictions.ipynb` — load a checkpoint, render prediction panels.

Keep heavy outputs stripped before committing (see `nbstripout` or the
`trailing-whitespace`/`check-added-large-files` pre-commit hooks).
