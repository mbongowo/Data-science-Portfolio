# Minimal pip fallback mirroring runtime deps from pyproject.toml.
# Prefer `pixi install` (conda-forge) for the geospatial stack; this is a fallback.
pystac-client>=0.7
planetary-computer>=1.0
odc-stac>=0.3
rioxarray>=0.15
xarray>=2024.1
dask>=2024.1
numpy>=1.26
spyndex>=0.6
rio-cogeo>=5.0
rasterio>=1.3
typer>=0.12
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6.0
matplotlib>=3.8
