name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dev tooling and the package
        run: |
          python -m pip install --upgrade pip
          # Install only what lint + the pure-pandas known-answer tests need.
          # dbt / duckdb / the orchestrator are exercised in the Docker/pixi env.
          pip install numpy pandas pyyaml pytest ruff mypy types-PyYAML pandas-stubs
          pip install --no-deps -e .

      - name: Ruff lint
        run: ruff check .

      - name: Ruff format check
        run: ruff format --check .

      - name: mypy (core, pure-python)
        run: mypy src/dwh/dq.py src/dwh/dimensional.py src/dwh/__init__.py

      - name: Pytest (known-answer data-quality core)
        run: pytest -q
